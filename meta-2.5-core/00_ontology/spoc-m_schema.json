{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://wekaou.org/schemas/spoc-m/2.5.0",
  "title": "SPOC-M Knowledge Representation",
  "description": "The 211-field schema for WEKAOU knowledge representation (DRAFT - Incomplete)",
  "type": "object",
  "required": ["subject", "predicate", "object"],
  "properties": {
    "subject": {
      "$ref": "#/definitions/Entity",
      "description": "The entity being described"
    },
    "predicate": {
      "$ref": "#/definitions/Relation",
      "description": "The relationship or property"
    },
    "object": {
      "$ref": "#/definitions/Value",
      "description": "The value or target entity"
    },
    "context": {
      "$ref": "#/definitions/Context",
      "description": "Spatial, temporal, and situational context"
    },
    "mechanism": {
      "$ref": "#/definitions/Mechanism",
      "description": "Provenance and derivation information"
    }
  },
  "definitions": {
    "Entity": {
      "type": "object",
      "description": "[DRAFT] Full schema pending - see documentation",
      "properties": {
        "id": {"type": "string"},
        "type": {"type": "string"},
        "label": {"type": "string"}
      }
    },
    "Relation": {
      "type": "object",
      "description": "[DRAFT] Full schema pending",
      "properties": {
        "type": {"type": "string"},
        "semantics": {"type": "string"}
      }
    },
    "Value": {
      "description": "[DRAFT] Full schema pending"
    },
    "Context": {
      "type": "object",
      "description": "[DRAFT] Full schema pending",
      "properties": {
        "spatial": {"type": "object"},
        "temporal": {"type": "object"},
        "epistemic": {"type": "object"}
      }
    },
    "Mechanism": {
      "type": "object",
      "description": "[DRAFT] Full schema pending",
      "properties": {
        "source": {"type": "string"},
        "derivation": {"type": "string"},
        "confidence": {"type": "number"}
      }
    }
  }
}
